# Database Configuration
DB_SERVER=helpdesk-sql-server-2025.database.windows.net
DB_NAME=helpdesk_db
DB_USER=helpdeskadmin
DB_PASSWORD=TempPassword123!

# Server Configuration
PORT=3001
NODE_ENV=development

# Azure Storage (for file attachments - we'll implement this later)
AZURE_STORAGE_CONNECTION_STRING=your-azure-storage-connection-string
AZURE_STORAGE_CONTAINER_NAME=ticket-attachments

# JWT Configuration (for when we add authentication)
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=24h